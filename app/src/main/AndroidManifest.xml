<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.entrenapp">

    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Entrenapp"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".DescriptionFragments.DescriptionActivity"
            android:exported="true"
            android:parentActivityName=".bodyActivity.BodyActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="entrenapp.com"
                    android:pathPrefix="/routine"
                    android:scheme="http"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".DescriptionFragments.ExerciseDescriptionActivity"
            android:exported="false"
            />

        <activity
            android:name=".executeRoutineActivity.ExecuteRoutineActivity"
            android:exported="false" />
        <activity
            android:name=".bodyActivity.BodyActivity"
            android:exported="false" />
        <activity
            android:name=".mainActivity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>